{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "launch",
            "type": "lldb",
            "request": "launch",

            "program": "${workspaceFolder}/build/tests/main",
            "cwd": "${workspaceFolder}/build/tests",

            "externalConsole": false,

            "env": {
                "VK_ICD_FILENAMES": "/usr/share/vulkan/icd.d/radeon_icd.x86_64.json"
            },

            "preLaunchTask": "build project",
        }
    ]
}
